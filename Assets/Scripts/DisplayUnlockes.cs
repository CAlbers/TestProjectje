using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisplayUnlockes : MonoBehaviour
{

    private Color missing = new Color(1, 1, 1, 0.2f);
    private Color available = new Color(1, 1, 1, 0.2f);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
